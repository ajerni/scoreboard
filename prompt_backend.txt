I want to create a scoreboard table on my postgres 16 database that works as a leaderboard. Use views, triggers and functions to create a scoreboard table that gets entries with name (string), score (int) and automatically updates the top 10 scores as soon as a new entry is done.  Then in the main.rs under /api routes I want 3 new endpoints that interact wiht this new scoreboard: enter_score, show_scores, empty_scoreboard. first give me the SQL command for psql to generate this new table.